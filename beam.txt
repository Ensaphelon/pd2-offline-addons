# The light over a drop. Re-read while the game runs — save this file and the next frame uses it.

art     beam      # beam (the Horadric shaft), jet (rays fanning out of the ground), or both
trans   5         # the draw's brightness argument; d2bs passes 5 for a blended image
bright  -1        # its other one
draw    10041     # which D2Gfx call does the drawing
rate    3         # game frames per sprite frame — lower is faster
dx      0         # nudge right
dy      0         # nudge down
anchor  offset    # offset (follows the view, including when a panel slides it) or player
on      1
test    1         # draw one at a fixed spot near the left edge, to judge the art safely
